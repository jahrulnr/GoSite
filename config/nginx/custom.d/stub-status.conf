# Internal nginx stub_status — localhost only (polled by GoSite nginxlite collector).
# See docs/sequences/22-nginx-metrics.md

server {
	listen 127.0.0.1:18081;
	server_name localhost;

	location /nginx_status {
		stub_status;
		allow 127.0.0.1;
		deny all;
	}
}

# Tier 1 MCP template

Copy to `plugins/<vendor>/<name>/` and customize `manifest.json`.

```bash
cp -r plugins/_templates/tier1-mcp plugins/acme/my-mcp
```

Build produces `plugin/gosite`, `plugin/validate`, and `plugin/mcp` in the zip artifact.

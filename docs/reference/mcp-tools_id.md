# Tool MCP & plugin `gosite/mcp`

Registry tool MCP, mapping scope, manifest plugin katalog.

**Status:** Desain — P6b

**Setup operator:** [mcp-operator.md](../guides/mcp-operator.md) · **Token:** [integration-tokens.md](./integration-tokens.md)

## Tool MVP

`system`, `websites`, `nginx`, `docker`, `jobs`, `plugins` — `tools/list` dinamis sesuai scope token.

**Re-introspect:** lazy on 403/401; retry hanya jika tool masih di registry setelah rebuild.

Detail lengkap: [mcp-tools.md](./mcp-tools.md) (EN).

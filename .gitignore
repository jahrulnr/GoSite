# Binaries
/bin/
*.exe
*.out
coverage.out

# Bundled plugin release artifacts (built via make bundled-plugins)
dist/bundled-plugins/

# Local runtime data
/data/
/logs/

# Frontend
web/node_modules/
web/tsconfig.tsbuildinfo

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Env secrets
.env
.env.*

# Generated GitHub wiki export (source: docs/ → make wiki-export)
docs/wiki-export/
research/

# Local deploy / cloud lab (never commit — host/IP/VM ids)
scripts/*.local.sh
scripts/cloud-lab.local.sh
deploy.local.env
gosite.tar.gz
gosite-cloud-lab.tar.gz
compose.bangunsoft.yml
